# Run report - s4_r1

## Loop stages executed by the agent

- **Inspect data (EDA):** completed at iteration #0 - the agent wrote and ran its own exploratory script; its findings are in `eda_report.txt` and were carried into every later prompt.
- **Reproduce official baseline (Requirement 1):** 1 attempt(s); the agent's own pipeline reached validation primary **0.6026** against the official 0.6016 (delta +0.0010, inside the baseline's 5-seed noise). That script became the root of the search tree.
- **Iterate:** 16 experiments proposed, executed and evaluated by the agent, each branching from a node of its own search tree.

## Iteration log

A turn is one hypothesis-to-score cycle and advances several lineages at once, so `turn` and `slot` say when an experiment ran and which lineage produced it. The convergence rule is measured against turns; the 50-cap counts scripts.

| # | turn | slot | phase | parent | status | secs | primary | vs baseline | hypothesis |
|---|---|---|---|---|---|---|---|---|---|
| 0 | 0 | - | eda | - | ok | 1 | n/a | - | In the data-inspection stage, temporal target-rate signal, category co |
| 1 | 0 | - | baseline | - | ok | 5 | 0.6026 | +0.0010 | Reproduce the official baseline end-to-end by training a rank-16 Facto |
| 2 | 1 | 0 | improve | #1 | kept | 55 | 0.6035 | +0.0019 | Target the training/feature stage with three structurally different fa |
| 3 | 1 | 1 | improve | #1 | kept | 55 | 0.6035 | +0.0019 | Target training under temporal drift by comparing recency-weighted bin |
| 4 | 1 | 2 | improve | #1 | kept | 28 | 0.6035 | +0.0019 | Target prediction formation and post-processing by comparing recency-w |
| 5 | 1 | 3 | improve | #1 | kept | 59 | 0.6044 | +0.0028 | Target the training objective and prediction family: compare recency-w |
| 6 | 2 | 0 | improve | #5 | ok | 21 | 0.6047 | +0.0031 | Target the sequential feature/model stage: a causal DIN using each use |
| 7 | 2 | 1 | improve | #5 | kept | 46 | 0.6047 | +0.0031 | Target the ranking objective and prediction-formation stages with a ha |
| 8 | 2 | 2 | improve | #5 | kept | 50 | 0.6048 | +0.0032 | Target prediction formation and drift-robust ensembling by comparing f |
| 9 | 2 | 3 | improve | #5 | kept | 7 | 0.6048 | +0.0032 | Target prediction formation under drift with three non-FM families—a r |
| 10 | 3 | 0 | improve | #8 | kept | 45 | 0.6048 | +0.0032 | Target session-context and ranking-objective stages: a recency-weighte |
| 11 | 3 | 1 | improve | #8 | kept | 49 | 0.6048 | +0.0032 | Target the objective and prediction-formation stages with three comple |
| 12 | 3 | 2 | improve | #8 | kept | 50 | 0.6048 | +0.0032 | Target prediction formation with three low-correlation families—explic |
| 13 | 3 | 3 | improve | #8 | kept | 2 | 0.6048 | +0.0032 | Target non-parametric prediction formation with generative categorical |
| 14 | 4 | 0 | improve | #9 | kept | 43 | 0.6048 | +0.0032 | Target query construction and objective under drift: candidate-set-rel |
| 15 | 4 | 1 | improve | #9 | kept | 28 | 0.6048 | +0.0032 | Target prediction formation under drift with an out-of-fold user-condi |
| 16 | 4 | 2 | improve | #9 | kept | 33 | 0.6048 | +0.0032 | Target temporal robustness and prediction formation by comparing domai |
| 17 | 4 | 3 | improve | #9 | kept | 3 | 0.6048 | +0.0032 | Target prediction formation with three non-parametric but structurally |

## Portfolio

- Lineages advanced per turn: **4**
- Turns: **4**  ·  scripts executed: **18** of 50
- A turn is one hypothesis-to-score cycle -- Figure 1's iteration, and what the convergence rule is measured against. Scripts are reported separately so the run can be checked against the stricter reading of the 50-iteration cap as well.
- Lineages archived: **4**  ·  revived from the archive: **1**

### Did the lineages actually disagree?

Within-user rank correlation between the live slots' validation predictions. This is the acceptance test for the whole design: above ~0.95 the extra slots are three copies of one agent and bought nothing.

| turn | scripts | mean corr | max corr | alert |
|---|---|---|---|---|
| 1 | #2, #3, #4, #5 | 0.7675 | 0.8536 | no |
| 2 | #6, #7, #8, #9 | 0.7844 | 0.8839 | no |
| 3 | #10, #11, #12, #13 | 0.8031 | 1.0000 | **YES** |
| 4 | #14, #15, #16, #17 | 0.7556 | 0.8094 | no |

### Lineages retired and replaced

A slot that stops beating its own best is archived, not stopped -- the run's convergence counter is untouched by it. Refill alternates fresh drafts with a revival, and a revival is chosen for score AND for disagreeing with what is already live.

| turn | slot | archived at | choice | detail |
|---|---|---|---|---|
| 2 | 0 | 0.6047 | fresh | new draft |
| 2 | 1 | 0.6047 | fresh | new draft |
| 2 | 2 | 0.6048 | fresh | new draft |
| 2 | 3 | 0.6048 | revived | entry #2 (primary 0.6048) |

### Controller blend of the portfolio

Every turn the controller blends the trusted incumbent, the live slots and the archive, choosing members on one half of validation and confirming on the other. A blend that wins the selection fold and loses the confirmation fold is refused: it won the split, not the task.

| turn | pool | accepted | members | fold A | fold B | reason |
|---|---|---|---|---|---|---|
| 1 | 4 | no | - | - | - | no member improved fold A |
| 2 | 4 | no | - | - | - | no member improved fold A |
| 3 | 8 | no | - | - | - | no member improved fold A |
| 4 | 8 | no | - | - | - | no member improved fold A |

## Code diff applied, per iteration

Each iteration edits a parent script. This is that edit, reconstructed from the two full sources in the ledger, truncated to the first 40 lines per iteration.


**#1 -> #2**  (+400 / -107 lines)

```diff
--- iter_1.py
+++ iter_2.py
@@ -1,117 +1,411 @@
 import os
 import time
+import json
 import random
+import gc
+
 import numpy as np
 import torch
 import torch.nn as nn
 import torch.nn.functional as F
+import lightgbm as lgb
 
 from pipeline.data import load, FEATURE_CARDINALITIES
+from pipeline.history import historical_features
 from pipeline.evaluate import evaluate
 
 
 START = time.time()
-SEED = 2024
-FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
-K = 16
-LR = 0.001
-BATCH_SIZE = 2048
-EPOCHS = 5
+SEED = 7319
+THREADS = min(8, os.cpu_count() or 1)
+HALF_LIFE = 4.0
 
 random.seed(SEED)
 np.random.seed(SEED)
 torch.manual_seed(SEED)
-torch.set_num_threads(min(8, os.cpu_count() or 1))
-
-
-cardinalities = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
-offsets = np.cumsum([0] + cardinalities[:-1], dtype=np.int64)
-total_cardinality = int(sum(cardinalities))
... 505 more diff lines
```

**#1 -> #3**  (+330 / -109 lines)

```diff
--- iter_1.py
+++ iter_3.py
@@ -1,117 +1,321 @@
 import os
 import time
-import random
+import json
+import gc
 import numpy as np
-import torch
-import torch.nn as nn
-import torch.nn.functional as F
-
-from pipeline.data import load, FEATURE_CARDINALITIES
+import lightgbm as lgb
+
+from pipeline.data import load
+from pipeline.history import historical_features
 from pipeline.evaluate import evaluate
 
 
 START = time.time()
-SEED = 2024
-FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
-K = 16
-LR = 0.001
-BATCH_SIZE = 2048
-EPOCHS = 5
-
-random.seed(SEED)
-np.random.seed(SEED)
-torch.manual_seed(SEED)
-torch.set_num_threads(min(8, os.cpu_count() or 1))
-
-
-cardinalities = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
-offsets = np.cumsum([0] + cardinalities[:-1], dtype=np.int64)
-total_cardinality = int(sum(cardinalities))
-
-
... 425 more diff lines
```

**#1 -> #4**  (+394 / -108 lines)

```diff
--- iter_1.py
+++ iter_4.py
@@ -1,4 +1,5 @@
 import os
 import time
+import json
 import random
 import numpy as np
@@ -12,11 +13,5 @@
 
 START = time.time()
-SEED = 2024
-FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
-K = 16
-LR = 0.001
-BATCH_SIZE = 2048
-EPOCHS = 5
-
+SEED = 314159
 random.seed(SEED)
 np.random.seed(SEED)
@@ -24,94 +19,374 @@
 torch.set_num_threads(min(8, os.cpu_count() or 1))
 
-
-cardinalities = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
-offsets = np.cumsum([0] + cardinalities[:-1], dtype=np.int64)
-total_cardinality = int(sum(cardinalities))
-
-
-def make_matrix(split):
-    cols = []
-    for field, offset, cardinality in zip(FIELDS, offsets, cardinalities):
-        x = np.asarray(split.X[field], dtype=np.int64)
-        if x.size and (x.min() < 0 or x.max() >= cardinality):
-            raise ValueError(
-                f"{field} contains IDs outside [0, {cardinality - 1}]"
-            )
-        cols.append(x + offset)
-    return np.ascontiguousarray(np.stack(cols, axis=1), dtype=np.int64)
... 493 more diff lines
```

**#1 -> #5**  (+354 / -81 lines)

```diff
--- iter_1.py
+++ iter_5.py
@@ -1,4 +1,5 @@
 import os
 import time
+import json
 import random
 import numpy as np
@@ -12,10 +13,7 @@
 
 START = time.time()
-SEED = 2024
+SEED = 2026
 FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
-K = 16
-LR = 0.001
-BATCH_SIZE = 2048
-EPOCHS = 5
+BATCH = 4096
 
 random.seed(SEED)
@@ -24,35 +22,98 @@
 torch.set_num_threads(min(8, os.cpu_count() or 1))
 
-
-cardinalities = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
-offsets = np.cumsum([0] + cardinalities[:-1], dtype=np.int64)
-total_cardinality = int(sum(cardinalities))
+train = load("train")
+valid = load("valid")
+test = load("test")
+
+cards = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
+offsets = np.cumsum([0] + cards[:-1], dtype=np.int64)
+total_cardinality = int(sum(cards))
 
 
 def make_matrix(split):
     cols = []
-    for field, offset, cardinality in zip(FIELDS, offsets, cardinalities):
... 441 more diff lines
```

**#5 -> #6**  (+493 / -341 lines)

```diff
--- iter_5.py
+++ iter_6.py
@@ -13,7 +13,8 @@
 
 START = time.time()
-SEED = 2026
+SEED = 7319
+HISTORY_LEN = 12
 FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
-BATCH = 4096
+BATCH_SIZE = 4096
 
 random.seed(SEED)
@@ -26,251 +27,335 @@
 test = load("test")
 
-cards = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
-offsets = np.cumsum([0] + cards[:-1], dtype=np.int64)
-total_cardinality = int(sum(cards))
-
-
-def make_matrix(split):
-    cols = []
-    for field, offset, card in zip(FIELDS, offsets, cards):
-        x = np.asarray(split.X[field], dtype=np.int64)
-        if x.size and (x.min() < 0 or x.max() >= card):
-            raise ValueError("%s has an out-of-range ID" % field)
-        cols.append(x + offset)
-    return np.ascontiguousarray(np.stack(cols, axis=1), dtype=np.int64)
-
-
-xtr = make_matrix(train)
-xva = make_matrix(valid)
-xte = make_matrix(test)
+cards = [int(FEATURE_CARDINALITIES[name]) for name in FIELDS]
+user_card = int(FEATURE_CARDINALITIES["user_id"])
+video_card = int(FEATURE_CARDINALITIES["video_id"])
+
+
+def make_x(split):
... 848 more diff lines
```

**#5 -> #7**  (+588 / -324 lines)

```diff
--- iter_5.py
+++ iter_7.py
@@ -13,7 +13,7 @@
 
 START = time.time()
-SEED = 2026
-FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
+SEED = 314159
 BATCH = 4096
+MAX_SLATE = 96
 
 random.seed(SEED)
@@ -21,4 +21,5 @@
 torch.manual_seed(SEED)
 torch.set_num_threads(min(8, os.cpu_count() or 1))
+rng = np.random.default_rng(SEED)
 
 train = load("train")
@@ -26,35 +27,126 @@
 test = load("test")
 
-cards = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
-offsets = np.cumsum([0] + cards[:-1], dtype=np.int64)
-total_cardinality = int(sum(cards))
-
-
-def make_matrix(split):
+ytr = np.asarray(train.y, dtype=np.float32)
+dates = np.asarray(train.date, dtype=np.int64)
+
+# Recency weighting is fixed before validation inspection. It makes training
+# examples near the date boundary dominate because the evaluation period
+# continues the observed temporal drift.
+row_weight = np.exp2(
+    (dates - dates.max()).astype(np.float32) / 4.0
+)
+row_weight /= row_weight.mean()
+row_weight = row_weight.astype(np.float32)
+
+
... 949 more diff lines
```

**#5 -> #8**  (+454 / -337 lines)

```diff
--- iter_5.py
+++ iter_8.py
@@ -13,7 +13,8 @@
 
 START = time.time()
-SEED = 2026
+SEED = 314159
 FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
-BATCH = 4096
+BATCH_SIZE = 4096
+PRED_BATCH_SIZE = 16384
 
 random.seed(SEED)
@@ -29,248 +30,42 @@
 offsets = np.cumsum([0] + cards[:-1], dtype=np.int64)
 total_cardinality = int(sum(cards))
+n_fields = len(FIELDS)
 
 
 def make_matrix(split):
-    cols = []
+    columns = []
     for field, offset, card in zip(FIELDS, offsets, cards):
-        x = np.asarray(split.X[field], dtype=np.int64)
-        if x.size and (x.min() < 0 or x.max() >= card):
-            raise ValueError("%s has an out-of-range ID" % field)
-        cols.append(x + offset)
-    return np.ascontiguousarray(np.stack(cols, axis=1), dtype=np.int64)
-
-
-xtr = make_matrix(train)
-xva = make_matrix(valid)
-xte = make_matrix(test)
-ytr = np.asarray(train.y, dtype=np.float32)
-
-# Fixed four-day half-life, motivated by the known continuation of temporal drift.
-dates = np.asarray(train.date, dtype=np.int64)
-sample_weight = np.exp2((dates - dates.max()).astype(np.float32) / 4.0)
+        values = np.asarray(split.X[field], dtype=np.int64)
+        if values.size and (values.min() < 0 or values.max() >= card):
... 806 more diff lines
```

**#5 -> #9**  (+527 / -365 lines)

```diff
--- iter_5.py
+++ iter_9.py
@@ -7,4 +7,6 @@
 import torch.nn as nn
 import torch.nn.functional as F
+import scipy.sparse as sp
+from scipy.sparse.linalg import svds
 
 from pipeline.data import load, FEATURE_CARDINALITIES
@@ -13,7 +15,6 @@
 
 START = time.time()
-SEED = 2026
-FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
-BATCH = 4096
+SEED = 7319
+BATCH = 8192
 
 random.seed(SEED)
@@ -26,360 +27,514 @@
 test = load("test")
 
-cards = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
-offsets = np.cumsum([0] + cards[:-1], dtype=np.int64)
-total_cardinality = int(sum(cards))
-
-
-def make_matrix(split):
-    cols = []
-    for field, offset, card in zip(FIELDS, offsets, cards):
+ytr = np.asarray(train.y, dtype=np.float32)
+dates = np.asarray(train.date, dtype=np.int64)
+
+# A fixed four-day half-life was motivated before this iteration by the
+# measured date drift. It is applied to every supervised/statistical family.
+recency_weight = np.exp2(
+    (dates - dates.max()).astype(np.float32) / 4.0
+)
+recency_weight /= recency_weight.mean()
+recency_weight = recency_weight.astype(np.float32)
... 894 more diff lines
```

**#8 -> #10**  (+477 / -459 lines)

```diff
--- iter_8.py
+++ iter_10.py
@@ -6,16 +6,13 @@
 import torch
 import torch.nn as nn
-import torch.nn.functional as F
-
-from pipeline.data import load, FEATURE_CARDINALITIES
+import lightgbm as lgb
+
+from pipeline.data import load
+from pipeline.history import historical_features
 from pipeline.evaluate import evaluate
 
 
 START = time.time()
-SEED = 314159
-FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
-BATCH_SIZE = 4096
-PRED_BATCH_SIZE = 16384
-
+SEED = 271828
 random.seed(SEED)
 np.random.seed(SEED)
@@ -27,45 +24,6 @@
 test = load("test")
 
-cards = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
-offsets = np.cumsum([0] + cards[:-1], dtype=np.int64)
-total_cardinality = int(sum(cards))
-n_fields = len(FIELDS)
-
-
-def make_matrix(split):
-    columns = []
-    for field, offset, card in zip(FIELDS, offsets, cards):
-        values = np.asarray(split.X[field], dtype=np.int64)
-        if values.size and (values.min() < 0 or values.max() >= card):
-            raise ValueError("Out-of-range IDs in " + field)
-        columns.append(values + offset)
... 947 more diff lines
```

**#8 -> #11**  (+421 / -375 lines)

```diff
--- iter_8.py
+++ iter_11.py
@@ -13,8 +13,8 @@
 
 START = time.time()
-SEED = 314159
+SEED = 271828
 FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
-BATCH_SIZE = 4096
-PRED_BATCH_SIZE = 16384
+BATCH_SIZE = 8192
+PRED_BATCH_SIZE = 32768
 
 random.seed(SEED)
@@ -27,29 +27,8 @@
 test = load("test")
 
-cards = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
-offsets = np.cumsum([0] + cards[:-1], dtype=np.int64)
-total_cardinality = int(sum(cards))
-n_fields = len(FIELDS)
-
-
-def make_matrix(split):
-    columns = []
-    for field, offset, card in zip(FIELDS, offsets, cards):
-        values = np.asarray(split.X[field], dtype=np.int64)
-        if values.size and (values.min() < 0 or values.max() >= card):
-            raise ValueError("Out-of-range IDs in " + field)
-        columns.append(values + offset)
-    return np.ascontiguousarray(np.stack(columns, axis=1), dtype=np.int64)
-
-
-x_train = make_matrix(train)
-x_valid = make_matrix(valid)
-x_test = make_matrix(test)
 y_train = np.asarray(train.y, dtype=np.float32)
-
-# The evaluation windows follow immediately after training, while the label
-# rate and activity distribution drift substantially. A four-day half-life
... 848 more diff lines
```

**#8 -> #12**  (+272 / -354 lines)

```diff
--- iter_8.py
+++ iter_12.py
@@ -13,8 +13,16 @@
 
 START = time.time()
-SEED = 314159
-FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
+SEED = 8675309
 BATCH_SIZE = 4096
-PRED_BATCH_SIZE = 16384
+PRED_BATCH_SIZE = 32768
+FIELDS = [
+    "user_id",
+    "video_id",
+    "author_id",
+    "tab",
+    "tag",
+    "duration_bucket",
+    "hour",
+]
 
 random.seed(SEED)
@@ -27,18 +35,18 @@
 test = load("test")
 
-cards = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
+y_train = np.asarray(train.y, dtype=np.float32)
+cards = [int(FEATURE_CARDINALITIES[name]) for name in FIELDS]
 offsets = np.cumsum([0] + cards[:-1], dtype=np.int64)
 total_cardinality = int(sum(cards))
-n_fields = len(FIELDS)
 
 
 def make_matrix(split):
-    columns = []
-    for field, offset, card in zip(FIELDS, offsets, cards):
-        values = np.asarray(split.X[field], dtype=np.int64)
+    cols = []
+    for name, card, offset in zip(FIELDS, cards, offsets):
+        values = np.asarray(split.X[name], dtype=np.int64)
... 699 more diff lines
```

**#8 -> #13**  (+414 / -457 lines)

```diff
--- iter_8.py
+++ iter_13.py
@@ -2,9 +2,5 @@
 import time
 import json
-import random
 import numpy as np
-import torch
-import torch.nn as nn
-import torch.nn.functional as F
 
 from pipeline.data import load, FEATURE_CARDINALITIES
@@ -13,13 +9,6 @@
 
 START = time.time()
-SEED = 314159
-FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
-BATCH_SIZE = 4096
-PRED_BATCH_SIZE = 16384
-
-random.seed(SEED)
-np.random.seed(SEED)
-torch.manual_seed(SEED)
-torch.set_num_threads(min(8, os.cpu_count() or 1))
+HALF_LIFE = 4.0
+SMOOTH = 20.0
 
 train = load("train")
@@ -27,52 +16,37 @@
 test = load("test")
 
-cards = [int(FEATURE_CARDINALITIES[f]) for f in FIELDS]
-offsets = np.cumsum([0] + cards[:-1], dtype=np.int64)
-total_cardinality = int(sum(cards))
-n_fields = len(FIELDS)
-
-
-def make_matrix(split):
-    columns = []
-    for field, offset, card in zip(FIELDS, offsets, cards):
... 899 more diff lines
```

**#9 -> #14**  (+516 / -495 lines)

```diff
--- iter_9.py
+++ iter_14.py
@@ -7,6 +7,5 @@
 import torch.nn as nn
 import torch.nn.functional as F
-import scipy.sparse as sp
-from scipy.sparse.linalg import svds
+import lightgbm as lgb
 
 from pipeline.data import load, FEATURE_CARDINALITIES
@@ -15,6 +14,6 @@
 
 START = time.time()
-SEED = 7319
-BATCH = 8192
+SEED = 18437
+BATCH = 16384
 
 random.seed(SEED)
@@ -28,197 +27,138 @@
 
 ytr = np.asarray(train.y, dtype=np.float32)
-dates = np.asarray(train.date, dtype=np.int64)
-
-# A fixed four-day half-life was motivated before this iteration by the
-# measured date drift. It is applied to every supervised/statistical family.
-recency_weight = np.exp2(
-    (dates - dates.max()).astype(np.float32) / 4.0
-)
-recency_weight /= recency_weight.mean()
-recency_weight = recency_weight.astype(np.float32)
-
-
-def within_user_rank(user_ids, scores):
-    user_ids = np.asarray(user_ids, dtype=np.int64)
-    scores = np.asarray(scores, dtype=np.float64)
-    n = len(scores)
-
-    # Deterministic row-position tie breaking.
-    order = np.lexsort((
... 1034 more diff lines
```

**#9 -> #15**  (+484 / -499 lines)

```diff
--- iter_9.py
+++ iter_15.py
@@ -7,6 +7,5 @@
 import torch.nn as nn
 import torch.nn.functional as F
-import scipy.sparse as sp
-from scipy.sparse.linalg import svds
+import lightgbm as lgb
 
 from pipeline.data import load, FEATURE_CARDINALITIES
@@ -15,6 +14,7 @@
 
 START = time.time()
-SEED = 7319
-BATCH = 8192
+SEED = 18437
+BATCH_SIZE = 8192
+FIELDS = ["user_id", "video_id", "author_id", "tab", "duration_bucket"]
 
 random.seed(SEED)
@@ -27,14 +27,183 @@
 test = load("test")
 
-ytr = np.asarray(train.y, dtype=np.float32)
-dates = np.asarray(train.date, dtype=np.int64)
-
-# A fixed four-day half-life was motivated before this iteration by the
-# measured date drift. It is applied to every supervised/statistical family.
-recency_weight = np.exp2(
-    (dates - dates.max()).astype(np.float32) / 4.0
-)
-recency_weight /= recency_weight.mean()
-recency_weight = recency_weight.astype(np.float32)
+y_all = np.asarray(train.y, dtype=np.float32)
+train_dates = np.asarray(train.date, dtype=np.int64)
+
+# Base models use 20220409-18. The gate is trained on 19-20 and assessed on
+# 21, so neither its architecture nor the chosen expert is selected on valid.
+base_mask = train_dates <= 20220418
+gate_train_mask = (train_dates >= 20220419) & (train_dates <= 20220420)
... 993 more diff lines
```

**#9 -> #16**  (+531 / -438 lines)

```diff
--- iter_9.py
+++ iter_16.py
@@ -7,6 +7,5 @@
 import torch.nn as nn
 import torch.nn.functional as F
-import scipy.sparse as sp
-from scipy.sparse.linalg import svds
+import lightgbm as lgb
 
 from pipeline.data import load, FEATURE_CARDINALITIES
@@ -15,5 +14,5 @@
 
 START = time.time()
-SEED = 7319
+SEED = 28471
 BATCH = 8192
 
@@ -28,13 +27,47 @@
 
 ytr = np.asarray(train.y, dtype=np.float32)
-dates = np.asarray(train.date, dtype=np.int64)
-
-# A fixed four-day half-life was motivated before this iteration by the
-# measured date drift. It is applied to every supervised/statistical family.
+train_dates = np.asarray(train.date, dtype=np.int64)
+max_train_date = int(train_dates.max())
+
+# Dates are consecutive inside this split, but YYYYMMDD subtraction is not
+# generally a valid day difference. Mapping through the observed train dates
+# avoids relying on that representation.
+unique_train_dates = np.sort(np.unique(train_dates))
+date_to_index = {
+    int(date): index for index, date in enumerate(unique_train_dates)
+}
+train_day = np.asarray(
+    [date_to_index[int(date)] for date in train_dates],
+    dtype=np.int64,
+)
+last_train_day = int(train_day.max())
+
... 1034 more diff lines
```

**#9 -> #17**  (+414 / -494 lines)

```diff
--- iter_9.py
+++ iter_17.py
@@ -2,24 +2,10 @@
 import time
 import json
-import random
 import numpy as np
-import torch
-import torch.nn as nn
-import torch.nn.functional as F
-import scipy.sparse as sp
-from scipy.sparse.linalg import svds
 
 from pipeline.data import load, FEATURE_CARDINALITIES
 from pipeline.evaluate import evaluate
 
-
 START = time.time()
-SEED = 7319
-BATCH = 8192
-
-random.seed(SEED)
-np.random.seed(SEED)
-torch.manual_seed(SEED)
-torch.set_num_threads(min(8, os.cpu_count() or 1))
 
 train = load("train")
@@ -27,14 +13,11 @@
 test = load("test")
 
-ytr = np.asarray(train.y, dtype=np.float32)
+y = np.asarray(train.y, dtype=np.float64)
 dates = np.asarray(train.date, dtype=np.int64)
 
-# A fixed four-day half-life was motivated before this iteration by the
-# measured date drift. It is applied to every supervised/statistical family.
-recency_weight = np.exp2(
-    (dates - dates.max()).astype(np.float32) / 4.0
-)
-recency_weight /= recency_weight.mean()
... 936 more diff lines
```

## Search tree

Each line is an executed script; indentation is the edit it was derived from. A node marked `[retired]` produced three children that failed to improve on it, so the search abandoned that branch and backtracked.

```
#1 0.6026 [retired]  Reproduce the official baseline end-to-end by training a rank-16 Facto
  #2 0.6035  Target the training/feature stage with three structurally different fa
  #3 0.6035  Target training under temporal drift by comparing recency-weighted bin
  #4 0.6035  Target prediction formation and post-processing by comparing recency-w
  #5 0.6044 [retired]  Target the training objective and prediction family: compare recency-w
    #6 0.6047  Target the sequential feature/model stage: a causal DIN using each use
    #7 0.6047  Target the ranking objective and prediction-formation stages with a ha
    #8 0.6048 [retired]  Target prediction formation and drift-robust ensembling by comparing f
      #10 0.6048  Target session-context and ranking-objective stages: a recency-weighte
      #11 0.6048  Target the objective and prediction-formation stages with three comple
      #12 0.6048  Target prediction formation with three low-correlation families—explic
      #13 0.6048  Target non-parametric prediction formation with generative categorical
    #9 0.6048 [retired]  Target prediction formation under drift with three non-FM families—a r
      #14 0.6048  Target query construction and objective under drift: candidate-set-rel
      #15 0.6048  Target prediction formation under drift with an out-of-fold user-condi
      #16 0.6048  Target temporal robustness and prediction formation by comparing domai
      #17 0.6048  Target prediction formation with three non-parametric but structurally
```

## What the agent established

The agent's belief set, revised after every scored iteration rather than appended to, so later evidence can demote an earlier conclusion instead of piling up beside it. A claim marked `(invalidated)` was contradicted by a later result. Machine-readable form with per-claim evidence in `knowledge.json`.

```
- (active) A rank-16 Factorization Machine trained with Adam at learning rate 0.001 on user_id, video_id, author_id, tab, and duration_bucket reproduces the official baseline within seed noise: 0.6026 versus 0.6016, a difference of 0.0010. [iters 1]
- (active) The first experiments from all four lineages were within seed noise of the reproduced 0.6026 FM baseline: slots 0–2 scored 0.6035 and slot 3 scored 0.6044. [iters 1,2]
- (active) Across iterations 3–5, the tested DIN, RankNet, LightGBM, drift, interaction, gating, neural, non-parametric, and ensemble variants scored 0.6047–0.6048; their mutual differences of at most 0.0001 are not measurable, while they are 0.0021–0.0022 above the reproduced FM baseline. [iters 3,4,5]
- (active) In iteration 5, candidate-set-relative equal-query ListNet, an out-of-fold user-conditional LambdaRank gate, drift-robust neural/recency methods, and structurally distinct non-parametric recommenders all scored 0.6048, producing no measurable gain over the prior best. [iters 5]
- (qualified) The iteration-4 ranking identity between slots 1 and 3 was not stable across proposals: iteration-5 pairwise correlations range from 0.727 to 0.809, although the mean correlation of 0.756 still indicates substantial prediction overlap. [iters 4,5]
- (active) Iteration 5 is the third consecutive turn without a gain above 0.002, so the stated stopping condition 
```

## Alternatives compared inside iterations

330 candidate solutions were built and scored across 16 iteration(s). The convergence rule charges one iteration per experiment, so searching inside an iteration buys comparisons the iteration budget cannot.

| iteration | candidates (validation primary) |
|---|---|
| #2 | deepfm_recent 0.5966, deepfm_recent_blend_0.05 0.6030, deepfm_recent_blend_0.10 0.6033, deepfm_recent_blend_0.16 0.6034, deepfm_recent_blend_0.23 0.6033, deepfm_recent_blend_0.32 0.6035, deepfm_recent_blend_0.43 0.6034, deepfm_recent_blend_0.56 0.6022 |
| #3 | binary_gbdt_2d_blend_0.25 0.6027, binary_gbdt_2d_blend_0.50 0.6000, binary_gbdt_2d_blend_0.75 0.5955, binary_gbdt_2d_standalone 0.5905, binary_gbdt_4d_blend_0.25 0.6024, binary_gbdt_4d_blend_0.50 0.5992, binary_gbdt_4d_blend_0.75 0.5949, binary_gbdt_4d_standalone 0.5895 |
| #4 | causal_sequence_recency 0.5711, causal_sequence_recency_blend_0.20 0.6027, causal_sequence_recency_blend_0.35 0.6005, causal_sequence_recency_blend_0.50 0.5948, causal_sequence_recency_blend_0.65 0.5878, content_profile_eb 0.5808, content_profile_eb_blend_0.20 0.6028, content_profile_eb_blend_0.35 0.6012 |
| #5 | dcnv2 0.6028, dcnv2_rankblend 0.6041, mmoe 0.6033, mmoe_rankblend 0.6044, pairwise_bpr 0.5769, pairwise_bpr_rankblend 0.5950, weighted_fm 0.5993, weighted_fm_rankblend 0.6019 |
| #6 | din 0.6036, din_blend_0.25 0.6043, din_blend_0.50 0.6044, din_blend_0.75 0.6040, item2vec_transition 0.5454, item2vec_transition_blend_0.25 0.6016, item2vec_transition_blend_0.50 0.5850, item2vec_transition_blend_0.75 0.5580 |
| #7 | hard_negative_ffm 0.4782, hard_negative_ffm_incblend_0.25 0.6004, hard_negative_ffm_incblend_0.50 0.5589, hard_negative_ffm_incblend_0.75 0.4979, setwise_transformer 0.6007, setwise_transformer_incblend_0.25 0.6043, setwise_transformer_incblend_0.50 0.6038, setwise_transformer_incblend_0.75 0.6019 |
| #8 | fibinet 0.6033, fibinet_blend_0.15 0.6041, fibinet_blend_0.25 0.6041, fibinet_blend_0.35 0.6041, fibinet_blend_0.5 0.6040, fibinet_slate_shrink_0.25 0.6041, fibinet_slate_shrink_0.4 0.6039, fibinet_slate_shrink_0.55 0.6038 |
| #9 | categorical_naive_bayes_blend_0.15 0.6040, categorical_naive_bayes_blend_0.30 0.6032, categorical_naive_bayes_blend_0.50 0.5993, categorical_naive_bayes_blend_0.70 0.5934, categorical_naive_bayes_blend_1.00 0.5903, categorical_naive_bayes_raw 0.5901, spectral_heterogeneous_blend_0.15 0.6037, spectral_heterogeneous_blend_0.30 0.6000 |
| #10 | new_ensemble_inc_blend_0.15 0.6046, new_ensemble_inc_blend_0.25 0.6043, new_ensemble_inc_blend_0.4 0.6030, new_ensemble_inc_blend_0.55 0.6002, new_family_ensemble 0.5837, positional_lightgbm 0.5810, positional_lightgbm_inc_blend_0.15 0.6046, positional_lightgbm_inc_blend_0.25 0.6035 |
| #11 | autoint_pointwise 0.5978, autoint_pointwise_incumbent_blend_0.15 0.6045, autoint_pointwise_incumbent_blend_0.25 0.6044, autoint_pointwise_incumbent_blend_0.35 0.6037, autoint_pointwise_incumbent_blend_0.5 0.6019, autoint_pointwise_incumbent_blend_0.65 0.6003, bpr_matrix_factorization 0.5845, bpr_matrix_factorization_incumbent_blend_0.15 0.6043 |
| #12 | categorical_naive_bayes 0.5923, categorical_naive_bayes_blend_0.1 0.6046, categorical_naive_bayes_blend_0.2 0.6043, categorical_naive_bayes_blend_0.35 0.6027, categorical_naive_bayes_blend_0.5 0.6005, multi_relational_mf 0.5960, multi_relational_mf_blend_0.1 0.6045, multi_relational_mf_blend_0.2 0.6045 |
| #13 | generative_naive_bayes 0.5898, generative_naive_bayes_rank_blend_0.1 0.6045, generative_naive_bayes_rank_blend_0.2 0.6042, generative_naive_bayes_rank_blend_0.3 0.6033, generative_naive_bayes_rank_blend_0.45 0.6013, generative_naive_bayes_rank_blend_0.6 0.5962, heterogeneous_graph_diffusion 0.5716, heterogeneous_graph_diffusion_rank_blend_0.1 0.6044 |
| #14 | query_additive_blend_0.15 0.6045, query_additive_blend_0.3 0.6042, query_additive_blend_0.5 0.6018, query_additive_blend_0.7 0.5984, query_additive_blend_1.0 0.5966, query_additive_raw 0.5965, set_context_gbdt_blend_0.15 0.6046, set_context_gbdt_blend_0.3 0.6040 |
| #15 | conditional_gate_fixed_blend 0.6040, conditional_gate_raw 0.6000, empirical_bayes_fixed_blend 0.6030, empirical_bayes_raw 0.5905, recent_fm_fixed_blend 0.6042, recent_fm_raw 0.6009, uniform_fm_fixed_blend 0.6036, uniform_fm_raw 0.6011 |
| #16 | domain_adversarial_neural_blend_0.15 0.6045, domain_adversarial_neural_blend_0.3 0.6046, domain_adversarial_neural_blend_0.5 0.6039, domain_adversarial_neural_blend_0.7 0.6036, domain_adversarial_neural_raw 0.6033, recency_random_forest_blend_0.15 0.6047, recency_random_forest_blend_0.3 0.6036, recency_random_forest_blend_0.5 0.6001 |
| #17 | cohort_conditioned_tables_blend_0.1 0.6045, cohort_conditioned_tables_blend_0.2 0.6043, cohort_conditioned_tables_blend_0.3 0.6035, cohort_conditioned_tables_blend_0.45 0.6028, cohort_conditioned_tables_raw 0.5935, heterogeneous_nonparametric_blend_0.1 0.6045, heterogeneous_nonparametric_blend_0.2 0.6042, heterogeneous_nonparametric_blend_0.3 0.6031 |

## Resource usage (Feasibility & Practicality)

- **Agent wall-clock to converged result: 0.44 h (26 min)**
- Total LLM tokens: **303,430** (190,625 in / 112,805 out), including the knowledge-revision stage
- Iterations used: **18 of 50** (17 accepted scores, 0 failed, 0 rejected)
- Compute inside generated scripts: **0.16 h (10 min)** on CPU.
- Mean tokens per iteration: 16,857
- Stop reason: `converged`

## Autonomy (Impact & Relevance)

- **Manual interventions: 0.** No human edited code, restarted the loop, chose a hypothesis, or selected a result during the run.
- The agent inspected the data, reproduced the baseline, and chose every subsequent experiment itself. The prompt it starts from contains the task specification, the pipeline API and the output contract - no findings about what works on this dataset.
- Failures recovered by in-loop retry: 0
- Ideas retired after repeated failure or underperformance: 0

## Robustness (Technical Execution)

- **No failures occurred in this run.** The recovery machinery was therefore never exercised here; the evidence that it works is below.

The loop never stalled, crashed, or escalated to a human. Guards in place: retry-with-source on crash, distinct handling for timeouts (which are not bugs to fix), method-keyed retirement so a reworded idea cannot evade the blacklist, process-tree kill so a timed-out script leaves no orphan burning CPU, tolerance of LLM outages and rate limits, node retirement with backtracking so the search cannot grind on one exhausted branch, and a circuit breaker that halts on repeated instant failures (a broken environment rather than broken code).

### Evidence from development runs

Across 44 development runs of this agent, 394 iterations were executed and 28 failed. Every one was handled in-loop; none was escalated to a human. Failure taxonomy:

- `}`: 7
- `TIMEOUT`: 4
- `TypeError`: 4
- `ValueError`: 4
- `IndexError`: 2
- `AttributeError`: 2
- `TimeoutError`: 2
- `lightgbm.basic.LightGBMError`: 1

Each recovery path, with a concrete instance:

- **Retry with source.** `r38_1k` #9 crashed with `lightgbm.basic.LightGBMError: Number of rows 13924 exceeds upper limit`. The traceback *and the failing script* went back to the proposer, which fixed it: #10 scored 0.6704.
- **Timeout, handled as distinct from a bug.** `r38_1k` #11 was killed at the limit after 4373s. The feedback says the approach is too slow rather than wrong, because re-running a slow script unchanged just times out again.

## Result

- Best validation primary: **0.6048** (baseline 0.6016, delta +0.0032)
- From iteration #8: Target prediction formation and drift-robust ensembling by comparing field-aware factorization, SENET-bilinear CTR, wide-and-deep, and PLE multi-task models, then shrink each complementary model’s rank-blend weight for short user slates where its within-user ordering is least identifiable.
- Selection is on validation only, per the scoring rules; the hidden test set was never used to choose between iterations.
- Submission: written to `runs/slot_ladder/s4_r1/submission.csv`

### Results table

| split | GAUC | nDCG@5 | primary |
|---|---|---|---|
| validation (best iteration) | 0.6716 | 0.5380 | 0.6048 |
| official baseline (validation) | 0.6674 | 0.5357 | 0.6016 |
| hidden test (this submission) | 0.6657 | 0.5312 | **0.5985** |
| official baseline (test) | 0.6610 | 0.5282 | 0.5946 |

**Absolute delta over baseline on hidden test: GAUC +0.0047, nDCG@5 +0.0030, mean +0.0039** (primary +0.0039).

Per the scoring formula, delta(m) = score_agent(m) - score_baseline(m), averaged over metrics.
